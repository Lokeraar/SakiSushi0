Nothing should be ignored in this case, as the only change is adding a markdown configuration file (`CONFIGURACION-EMAILS.md`), which is a source/config file and not a build artifact, dependency, or temporary file.