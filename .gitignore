# Examples 1 and 2 demonstrate the expected behavior, but since the current change list only contains HTML files (source code), no .gitignore rules are needed.

# The modified files 'Cajero/index.html' and 'Cliente/index.html' are source files and should not be ignored.